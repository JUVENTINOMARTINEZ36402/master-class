services:
  master-class:
    container_name: master-class
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    command: npm run dev

volumes:
  node_modules:
